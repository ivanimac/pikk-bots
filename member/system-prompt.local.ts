/**
 * member/system-prompt.local.ts — Override OPCIONAL del system prompt.
 *
 * Si este archivo existe y exporta una función `override()`,
 * el bot usará ESE system prompt en vez del generado automáticamente.
 *
 * Déjalo vacío (o no lo crees) para usar el prompt por defecto
 * con los datos de config.local.ts.
 */

// export function override(defaultPrompt: string): string {
//   return defaultPrompt.replace('Amable y cercano', 'Extremadamente formal y ceremonioso');
// }
